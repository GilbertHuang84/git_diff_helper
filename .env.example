# GitLab 访问令牌 (需要 api 作用域)
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx

# 私有化 GitLab 基础地址 (不要漏掉 https://)
GITLAB_URL=https://gitlab.yourcompany.com

# 可选：连接超时设置 (秒)
GITLAB_TIMEOUT=30

# 可选：如果你是自签名证书，可以设为 False 跳过验证 (生产环境建议设为 True)
GITLAB_HTTP_VERIFY=True